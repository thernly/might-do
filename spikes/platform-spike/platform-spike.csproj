<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>watcher_spike</RootNamespace>

    <!-- Throwaway by definition: a spike exists to answer a question, not to
         be held to the standard the shipped projects are. -->
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
  </PropertyGroup>

</Project>
