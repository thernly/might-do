<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MightDo.App.ViewModels"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="600"
             x:Class="MightDo.App.Views.BoardView"
             x:DataType="vm:WorkspaceViewModel">

  <UserControl.Styles>
    <!-- Cards sit flat on the column, the way paper does, and only lift
         under the pointer. The lift is the design system's printed shadow:
         a hard ink rectangle 2px off-register, not a blur — which is also
         why it is the same ink in both themes. A soft drop shadow would
         say "material" where this system says "poster". -->
    <Style Selector="Border.card">
      <Setter Property="Background" Value="{DynamicResource AppCardBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource AppCardBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Padding" Value="10,8" />
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
    <Style Selector="Border.card:pointerover">
      <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
      <Setter Property="BoxShadow" Value="2 2 0 0 #2D233A" />
    </Style>
    <!-- The card the detail pane is showing. -->
    <Style Selector="Border.card.selected">
      <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
      <Setter Property="BorderThickness" Value="2" />
    </Style>
    <!-- A column is a slot cut into the page rather than a tray resting
         on it: sunken ground, hairline keyline, square. -->
    <Style Selector="Border.column">
      <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="0" />
      <Setter Property="Padding" Value="8" />
      <Setter Property="Margin" Value="0,0,10,0" />
      <Setter Property="Width" Value="260" />
    </Style>
  </UserControl.Styles>

  <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
    <ItemsControl ItemsSource="{Binding Columns}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal" />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>

      <ItemsControl.ItemTemplate>
        <DataTemplate x:DataType="vm:BoardColumnViewModel">
          <!-- The column itself accepts drops, which append to the bottom. -->
          <Border Classes="column" Name="ColumnRoot" Tag="{Binding StatusId}">
            <Grid RowDefinitions="Auto,*">
              <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="2,0,2,8">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6">
                  <Ellipse Classes="statusdot"
                           Classes.initial="{Binding IsInitialStatus}"
                           Classes.active="{Binding IsActiveStatus}"
                           Classes.final="{Binding IsFinalStatus}" />
                  <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding CountLabel}" Classes="meta"
                           VerticalAlignment="Center" />
              </Grid>

              <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel MinHeight="120">
                  <ItemsControl ItemsSource="{Binding Cards}">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate x:DataType="vm:BoardCardViewModel">
                        <!-- Each card is both draggable and a drop target;
                             dropping on a card inserts above it. -->
                        <Border Classes="card" Name="CardRoot" Tag="{Binding Id}"
                                Classes.selected="{Binding IsSelected}">
                          <StackPanel Spacing="6">
                            <TextBlock Text="{Binding Summary}" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Spacing="5">
                              <Border Classes="chip"
                                      Classes.low="{Binding IsLowPriority}"
                                      Classes.medium="{Binding IsMediumPriority}"
                                      Classes.high="{Binding IsHighPriority}"
                                      Classes.critical="{Binding IsCriticalPriority}">
                                <TextBlock Text="{Binding PriorityLabel}" FontSize="12" />
                              </Border>
                              <Border Classes="chip" IsVisible="{Binding HasCategory}">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                  <Ellipse Width="7" Height="7" VerticalAlignment="Center"
                                           Fill="{Binding CategoryBrush}" />
                                  <TextBlock Text="{Binding CategoryName}" FontSize="12" />
                                </StackPanel>
                              </Border>
                              <Border Classes="chip" IsVisible="{Binding HasSteps}">
                                <TextBlock Text="{Binding StepsLabel}" FontSize="12" />
                              </Border>
                            </StackPanel>
                            <TextBlock Text="{Binding DateLabel}" Classes="due"
                                       Classes.overdue="{Binding IsOverdue}"
                                       IsVisible="{Binding HasDate}" />
                          </StackPanel>
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>

                  <TextBlock Classes="meta" Text="Drop here" Margin="4,6"
                             IsVisible="{Binding IsEmpty}" />
                </StackPanel>
              </ScrollViewer>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </ScrollViewer>
</UserControl>
