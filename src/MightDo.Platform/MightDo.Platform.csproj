<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\MightDo.Core\MightDo.Core.csproj" />

    <!-- ShellNotifier is an implementation detail, but the one thing worth
         asserting about it — that a flood of helper output cannot wedge the
         reminder gate — can only be asserted from outside. -->
    <InternalsVisibleTo Include="MightDo.Platform.Tests" />
  </ItemGroup>

  <PropertyGroup>
    <RootNamespace>MightDo.Platform</RootNamespace>
  </PropertyGroup>

</Project>
